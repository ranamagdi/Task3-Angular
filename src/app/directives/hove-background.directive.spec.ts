import { HoveBackgroundDirective } from './hove-background.directive';

describe('HoveBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new HoveBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
